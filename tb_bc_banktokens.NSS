int StartingConditional(int itemid)
{
	return 1; //I want all to show for bank
	//object oPC = GetPCSpeaker();
	//string entry = GetLocalString(oPC, IntToString(itemid) + "banktoken");
	//return (entry != "");
}